# MealPY-Ann-Slope-Stability
ANN for slope stability (SRF) prediction, hyperparameter-tuned with 12 MealPy metaheuristics (GWO, WOA, PSO, ABC, etc.). Just change one line to switch optimizer. Includes data preprocessing, sample weighting, final training, metrics (R², RMSE, VAF), plots &amp; Linear Regression baseline. Clean, ready-to-runrs2_srf.xlsx required
